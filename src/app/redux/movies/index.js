import * as actions from './moviesActions';
import * as selectors from './moviesSelectors';
import * as types from './moviesTypes';

const moviesModule = { actions, selectors, types };

export default moviesModule;
